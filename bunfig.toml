[build]
entrypoints = ["./src/index.ts"]
outdir = "./dist"
target = "node"
minify = true
sourcemap = "external"

[test]
coverage = true

[install]
registry = "https://registry.npmjs.org/"
scope = "@bentonow"
exact = true
minimumReleaseAge = 604800
auto = "disable"
